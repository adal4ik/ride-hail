services:
  postgres:
    image: postgres:16-alpine
    container_name: ridehail-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-admin}
      POSTGRES_PASSWORD: ${DB_PASSWORDL-admin}
      POSTGRES_DB: ${DB_NAME:-ride-hail}
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-admin} -d ${POSTGRES_DB:-ride-hail}"]
      interval: 5s
      timeout: 3s
      retries: 10

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: ridehail-rabbitmq
    volumes:
      - ./rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf:ro
      - ./rabbitmq_definitions.json:/etc/rabbitmq/rabbitmq_definitions.json
    ports:
      - "5672:5672"    # AMQP
      - "15672:15672"  # Management UI
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  # ride-service:
  #   build:
  #     context: .
  #     dockerfile: docker/ride-service.Dockerfile
  #   env_file: .env
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     rabbitmq:
  #       condition: service_healthy
  #   ports:
  #     - "3000:${RIDE_HTTP_PORT}"

  # driver-location-service:
  #   build:
  #     context: .
  #     dockerfile: docker/driver-location-service.Dockerfile
  #   env_file: .env
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     rabbitmq:
  #       condition: service_healthy
  #   ports:
  #     - "3001:${DRIVER_HTTP_PORT}"

  # admin-service:
  #   build:
  #     context: .
  #     dockerfile: docker/admin-service.Dockerfile
  #   env_file: .env
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     rabbitmq:
  #       condition: service_healthy
  #   ports:
  #     - "3004:${ADMIN_HTTP_PORT}"
